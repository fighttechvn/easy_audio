class AndroidService {
  final String title;
  final String content;

  const AndroidService({required this.title, required this.content});

  @override
  String toString() => 'AndroidService(title: $title, content: $content)';
}
