name: Pipeline Lint and Test

on:
  # pull_request:
  # push:
  #   branches: [develop]
  workflow_dispatch:

jobs:
  # lint:
  #   runs-on: ubuntu-latest
  #   steps:
  #   - uses: actions/checkout@v2

  #   # Setup the flutter environtment
  #   - name: Install and set Flutter version
  #     uses: subosito/flutter-action@v2
  #     with:
  #       channel: 'stable'

  #   # Get flutter dependencies
  #   - name: Restore packages
  #     run: flutter pub get

  #   # Check for any formatting issue in the code
  #   - name: Check formatting issue
  #     run: flutter format --set-exit-if-changed .

  #   # Statically analyze the Dart code for any errors.
  #   # - name: Analyze
  #   #   run: flutter analyze .

  #   # Run widget Lint on main module.
  #   - name: Run Lint on app_main module
  #     working-directory: ./modules/app_main
  #     run: flutter analyze .

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: subosito/flutter-action@v1
        with:
          channel: "stable" # 'dev', 'alpha', default to: 'stable'
      - run: flutter pub get
      - run: flutter analyze
